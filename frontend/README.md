
  # kiosk prototype

  This is a code bundle for kiosk prototype. The original project is available at https://www.figma.com/design/d9sjbEVEps3Mud0WrdZ6LA/kiosk-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  